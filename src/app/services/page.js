import Services from '@/pages/Services'
import React from 'react'

export const metadata = {
    title: "Services",
    description:
        "TrioScript offers web development services including portfolio websites, business sites, and custom web applications.",
    keywords: [
        "web development services",
        "react development services",
        "custom web apps",
    ],
};

const page = () => {
    return (
        <div>
            <Services />
        </div>
    )
}

export default page
